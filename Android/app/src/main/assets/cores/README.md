# Hysera core assets

This directory is reserved for pinned, reviewed sing-box and Xray core assets.

The current application intentionally ships without native VPN cores. Add assets only through a reproducible GitHub Actions process or from pre-verified release assets. Do not build native binaries locally and do not download untrusted binaries.
